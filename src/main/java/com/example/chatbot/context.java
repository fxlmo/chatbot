package com.example.chatbot;

/**
 * List of all possible contexts. Context means that the chatbot can continue a conversation if necessary without being too restrictive)
 */
public enum context {
    none,
    admin_else,
    user_else,
    list,
}
